<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Screening/CDD is regulated financial-crime data; no data is ever sent
         anywhere except this app's own configured amlkit server. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Passport scan on Android 12 and below reads a picked image via the
         system photo picker, which does not require this permission; kept
         only for the direct-camera-capture path. -->
    <!-- Camera is optional, not a requirement to install the app: a device
         with no camera can still onboard customers, just without the scan
         shortcut (manual entry works everywhere). Without this, the CAMERA
         permission alone tells Play a camera is required, which wrongly
         excludes camera-less devices (some ChromeOS/tablet installs). -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".AmlkitApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Amlkit"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Amlkit">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
